# CyberWatch pipeline dependencies (pinned for reproducible CI runs).
# Bump via Dependabot PRs (.github/dependabot.yml).
requests==2.32.3
feedparser==6.0.11
google-generativeai==0.8.3

# Dev / CI only:
pytest==8.3.4
ruff==0.8.4
